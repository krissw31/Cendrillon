{% extends "base.html.twig" %}
 {% block body %}
     <div class="col-sm-8" id="post-container">
         <p style="background-color: #C82829">Voici les informations du post</p>
     <h2>{{ post.postTitle }} </h2> (Message posté le <small>{{ post.postDate|date("Y m d") }})</small>
    <p>{{ post.postContent }}par <!--convertir en string pour l'auteur--></p>
         <p style="background-color: #C82829">Modifier à l'intérieur des encadrers suivants puis valider.</p>


{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
     {{ form_start(form,{'attr':{"novalidate":"novalidate"}}) }}
     {{ form_widget(form) }}
     {{ form_end(form) }}
     </div>


 {% endblock %}